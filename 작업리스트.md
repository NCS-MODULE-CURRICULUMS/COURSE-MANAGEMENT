# 준비 교안 → 학생용 교안 전환 작업

> 얇은 「준비 교안」에 **실습**을 붙여, 비전공 훈련생이 강사 없이 혼자 따라할 수 있는
> 교안으로 바꾸는 작업입니다. 이 파일만 읽으면 다른 컴퓨터에서 이어서 할 수 있습니다.
>
> 마지막 갱신 · 2026-09-21 · 응용SW 미작성 15건 중 **11건 완료** (m19~m29).
> 다음은 **12번 소프트웨어공학 활용**

---

## 1. 지금 어디까지

| 교안 | 이름 | 크기 | 단계 | 실습 | 상태 |
|---|---|---:|---:|---:|---|
| **m01** | 화면 설계 | 108,867 | 13 | 13 | **완료** |
| **m02** | UI 디자인 | 95,572 | 16 | 16 | **완료** |
| **m03** | 화면 구현 | 101,298 | 16 | 16 | **완료** |
| **m04** | 요구사항 확인 | 144,254 | 16 | 18 | **완료** |
| **m05** | 데이터베이스 구현 | 83,245 | 16 | 16 | **완료** |
| **m06** | SQL 활용 | 70,540 | 14 | 14 | **완료** |
| **m07** | 개발 환경 운영 지원 | 73,736 | 16 | 16 | **완료** |
| **m08** | 프로그래밍 언어 활용 | 87,961 | 16 | 16 | **완료** |
| **m09** | 네트워크 프로그래밍 구현 | 111,150 | 16 | 16 | **완료** |
| **m10** | 인터페이스 구현 | 121,561 | 16 | 16 | **완료** |
| **m11** | 애플리케이션 설계 | 112,614 | 16 | 16 | **완료** |
| **m12** | 서버프로그램 구현 | 116,350 | 16 | 16 | **완료** |
| **m13** | 통합 구현 | 115,173 | 16 | 16 | **완료** |
| **m14** | 애플리케이션 테스트 수행 | 106,372 | 16 | 16 | **완료** |
| **m15** | 애플리케이션 배포 | 110,426 | 16 | 16 | **완료** |
| m16 | 공공데이터 융합 DBMS 구축 | 41,223 | 15 | 0 | 미착수 |
| m17 | 공공데이터 융합 실무 프로젝트 | 41,816 | 16 | 0 | 미착수 |
| **m18** | 데이터 입출력 구현 | 128,746 | 16 | 16 | **완료** (신규·과정 미편성) |
| **m19** | 개발자 환경 구축 | 111,232 | 16 | 16 | **완료** (신규·NCS 학습모듈에서 새로 만듦) |
| **m20** | 프로그래밍 언어 응용 | 107,264 | 16 | 16 | **완료** (신규·NCS 학습모듈에서 새로 만듦) |

**m18 데이터 입출력 구현** — c2 과정에 **편성되지 않은** 능력단위입니다.
다른 과정에 끼워 쓸 수 있도록 **모듈 단위로 독립**하게 만들었습니다.
`courses/c2.html` · `assets/modules-c2.js` · `modules/c2-*.html` 에는 **넣지 않았습니다**
(넣으면 c2 의 시간표를 잘못 표시하게 됩니다). `curriculum/web-app-20010202.html` 의
해당 줄에서 「확인」 단추로 이어집니다.

**c1 과정(생성형 AI)** — `modules/c1-m01` ~ `c1-m09` 아홉 모듈 전부 **교안 없음**.
`guides/g01` ~ `g03` 은 강사용 운영 가이드이지 모듈 교안이 아닙니다.

진행 순서는 **m02 → m16 · m17 → c1 과정(9개 모듈)**. 교안이 있는 12건(m01 · m03 ~ m15 · m18)은 **전부 완료**입니다.

---

## 2. 바로 다음에 할 일

**m13 통합 구현** — 16단계, 실습 **0개**. 1~3 은 **끝났습니다.**

| 항목 | 값 |
|---|---|
| 능력단위 | 통합 구현 `2001020206_23v6` · 5수준 |
| 평가 | 평가자 체크리스트 100점 · **채점 01 ~ 15** |
| 레벨 | ① 연계 데이터 구성 `01~05` 30점 · ② 연계 메커니즘 구성 `06~10` 35점 · ③ 내외부 연계 모듈 구현 `11~15` 35점 |
| 현재 | 36,882자 · 단계 16 · 그림 14 · 실습 0 |
| 검수 | `verify_guide.py` 의 `SCORE["m13"]` 등록 완료(`01`~`15` 두 자리). 지금 돌리면 「실습이 없는 단계 16개」 만 뜹니다 |

단계 이름은 st1 완성 산출물 · st2 EX01 · st3 연계의 전체 모습 · st4 요구사항 분석서 ·
st5 데이터 식별과 표준 · st6 연계 정의서 · st7 외부를 부르는 코드 · st8 연계 주기 ·
st9 연계 실패 처리 · st10 송수신 보안 · st11 논리·물리 환경 · st12 수신·변환·저장 ·
st13 연계 데이터 재제공 · st14 연동 데이터의 무결성 · st15 연계 테스트 · st16 산출물 정리.

**검증 환경은 이렇게 세우기로 정했습니다** (아직 안 만들었습니다)

| 무엇 | 어떻게 |
|---|---|
| 우리 쪽 앱 | m12 의 `scratchpad/m12/lib` (118 jar · Boot 4.1 / Framework 7.0.8) 를 그대로 복사해 `kr.link` 로 새로 만듭니다 |
| Security | 캐시에 `spring-security-{core,config,web} 7.1.0` 이 있습니다. 세 개만 더 넣으면 됩니다 |
| **남의 서버** | `com.sun.net.httpserver` 로 **의존성 없는 작은 자바 서버**를 따로 띄웁니다 |

남의 서버를 직접 만드는 까닭이 있습니다. st9(연계 실패 처리)와 st8(연계 주기)는
**상대가 500 을 내거나 늦게 답하는 상황**을 봐야 하는데, 공개 API 로는 그것을
마음대로 만들 수 없습니다. 작은 서버면 `?fail=1` · `?delay=3000` 같은 것을
붙여 **실패와 지연을 부르는 대로** 낼 수 있습니다.

그 다음

4. 레벨 단위로 나눠 실습을 쓰고, 넣을 때마다 `verify_guide.py` 와 `check_tags.py` 를 돌립니다.
5. 그림이 필요하면 `tools/guide/mkimg_m18.py` 를 본보기로 SVG 를 그립니다 (5절).

그 다음은 **응용SW엔지니어링 미작성 15건 → m16 · m17 → c1 과정(9개 모듈)** 입니다.
m01 화면 설계 · m03 화면 구현을 먼저 하는 까닭은 **c2 에 실제로 편성된 것** 이고,
m16 · m17 은 **공공데이터 융합** 쪽이라 성격이 다르기 때문입니다.

## 3. 작업 방식 — 이대로 하십시오

### 3.0 규율 둘 — 이 둘만은 예외 없이

**하나. 쓰기 전에 돌려 본다** (3.1)
**둘. 고쳤으면 검수하고 고친다** — 묻지 말고 아래 여섯을 돌립니다.

```bash
python tools/guide/check_tags.py guides/mNN.html   # 태그 짝
python tools/guide/verify_guide.py mNN             # 구조 · 링크 · 채점 항목
python tools/guide/audit.py mNN                    # 학생이 따라할 수 있는가
python tools/guide/plain.py mNN                    # 강조 서식
python tools/guide/voice.py mNN                    # 교재 문체인가
python .github/scripts/check_site.py               # 저장소 전체
```

`audit.py` 의 **«치명» 은 반드시 고칩니다.** «주의» 는 해당하면 고치고,
아니면(문서만 쓰는 실습에 「안 될 때」 가 없는 것처럼) 넘어갑니다.
고친 내용은 커밋 메시지에 **무엇이 왜 틀렸는지** 적습니다.

`voice.py` 는 교안이 교재처럼 읽히는지를 숫자로 봅니다.
2026-09-21 에 「교안 말투가 너무 AI 스럽다」는 지적을 받고 만들었습니다.
규칙만 적어 두었을 때는 m26 → m27 → m28 로 가면서 슬금슬금 돌아갔습니다.
산문의 줄표가 84 → 96 → 105 번으로 늘었고 설명 문단의 절반 이상이 정확히 3문장이었는데,
눈으로는 안 보이고 재야 보였습니다.

<b>구조가 멀쩡해도 학생이 못 따라할 수 있습니다.</b>
m07 ~ m13 일곱 교안이 「무엇을 받아야 시작되는지」 를 한 번도 안 밝히고 있었는데,
`verify_guide.py` 는 통과시켰고 사람 눈으로도 계속 지나쳤습니다.
`audit.py` 가 일곱 건을 한 번에 잡았습니다.

### 3.1 가장 중요한 규율 : 쓰기 전에 돌려 본다

교안에 들어가는 **모든 명령·출력·오류 메시지는 실제로 실행해서 얻은 것**입니다.
기억이나 짐작으로 적지 않습니다. 이 규율 덕분에 다음을 잡아냈습니다.

- `com.mysql.jdbc.Driver` 는 Connector/J 9.0 에서도 **경고만 내고 동작**합니다
  (「8 이상에서는 못 찾는다」고 적었다가 실행해 보고 고침)
- `reached end of file while parsing` 은 괄호가 빠진 자리가 아니라 **파일 맨 끝 줄**을 가리킵니다
- `git rm --cached` 로 지워도 `git show 첫커밋:파일` 로 비밀번호가 그대로 나옵니다
- MySQL 8.0 은 FK-before-PK 에 `ERROR 1215` 가 아니라 **`1822`** 를 냅니다

### 3.2 실습 한 개의 모양

```html
<div class="lab">
  <b class="t">실습 2-N</b>
  <b>제목 — 무엇을 하는지</b>

  <p><b>목적</b> — 어느 채점 항목 몇 점인지 먼저 밝힙니다.</p>

  <h5>절차 1 — …</h5>
  <p class="cmdline"><code>실제로 칠 명령</code></p>
  <p class="cmdline"><code>실제로 나온 출력</code></p>

  <table class="wide">… 이렇게 나오면 / 뜻과 할 일 …</table>

  <div class="warn">흔히 틀리는 자리. «일부러 틀려 보게» 시킵니다.</div>

  <p><b>제출물</b> — 무엇을 캡처해 내는지</p>
  <p><b>스스로 확인</b> — ① … ② … ③ … ④ …</p>
</div>
```

지키는 것

- **틀린 것을 일부러 시켜 봅니다.** 「`private` 을 지워 보십시오」, 「`mid-1` 을 `mid` 로
  바꿔 보십시오」. 말로 경고만 하면 넘어가지만 직접 보면 남습니다.
- **한꺼번에 짜게 하지 않습니다.** 별만 → 공백 붙이기 → 개행 고치기처럼 쪼갭니다.
- **채점 항목 번호와 배점을 실습 첫 줄에** 답니다. 어디에 시간을 쓸지 스스로 정하게 합니다.
- **캡처에 무엇이 보여야 하는지**까지 적습니다. 「Console 캡처」가 아니라
  「입력한 숫자가 프롬프트 옆에 함께 보이게」.
- 한 번만 돌린 결과는 근거가 얇다는 것을 알려 줍니다(높이 두 가지, 구매 두 번, `c1`·`c2`).

### 3.3 절대 하지 않는 것

- **화면 캡처 도구로 GUI 창을 찍지 않습니다.** 사용자의 화면을 빼앗고,
  브라우저 탭·PuTTY 저장 세션 같은 개인 정보가 함께 찍힙니다.
  그림이 필요하면 **실제 파일 내용·명령 출력으로 SVG 를 그려** 넣습니다
  (`guides/img/rq_*.svg` 가 그렇게 만든 것입니다).
- 원본 사이트(`my-web-common-lecture.github.io`) 를 링크하지 않습니다.

---

## 3.5 응용SW엔지니어링 미작성 15건 (A안)

세분류 **20010202 응용SW엔지니어링**은 능력단위 **27개**입니다.
그 가운데 **12개**는 준비 교안이 있어 전부 완료했고, **15개는 교안 자체가 없습니다.**
m18 처럼 **처음부터 만들어야** 합니다.

만드는 순서는 **수준 낮은 것부터**입니다. 비전공 학생이 듣는 순서이고,
앞이 뒤의 재료가 됩니다.

| # | 수준 | 능력단위 | 능력단위코드 | 상태 |
|---:|---:|---|---|---|
| 1 | 2 | 개발자 환경 구축 | 2001020233_23v5 | **완료 → guides/m19.html** (16/16 · 111,232자) |
| 2 | 3 | 프로그래밍 언어 응용 | 2001020230_23v5 | **완료 → guides/m20.html** (16/16 · 107,264자) |
| 3 | 3 | 응용SW 기초 기술 활용 | 2001020232_23v5 | **완료 → guides/m21.html** (16/16 · 103,388자) |
| 4 | 5 | 자료구조 활용 | 2001020235_23v1 | **완료 → guides/m22.html** (16/16 · 106,406자) |
| 5 | 5 | 제품소프트웨어 패키징 | 2001020209_23v6 | **완료 → guides/m23.html** (16/16 · 106,817자) |
| 6 | 5 | 애플리케이션 테스트 관리 | 2001020226_23v6 | **완료 → guides/m24.html** (16/16 · 111,678자) |
| 7 | 5 | 정보시스템 이행 | 2001020208_23v5 | **완료 → guides/m25.html** (16/16 · 120,279자) |
| 8 | 6 | 기능 모델링 | 2001020220_23v3 | **완료 → guides/m26.html** (16/16 · 108,823자) |
| 9 | 6 | 정적모델 설계 | 2001020222_23v6 | **완료 → guides/m27.html** (16/16 · 131,443자) |
| 10 | 6 | 동적모델 설계 | 2001020223_23v6 | **완료 → guides/m28.html** (16/16 · 126,973자) |
| 11 | 6 | 인터페이스 설계 | 2001020218_23v6 | **완료 → guides/m29.html** (16/16 · 106,951자 · voice 0) |
| 12 | 6 | 소프트웨어공학 활용 | 2001020228_23v6 | 미착수 |
| 13 | 6 | 소프트웨어개발 방법론 활용 | 2001020229_23v6 | 미착수 |
| 14 | 6 | 애플리케이션 리팩토링 | 2001020217_23v6 | 미착수 |
| 15 | 7 | 애플리케이션 요구사항 분석 | 2001020219_23v6 | 미착수 |

**A안** — 지금까지와 **같은 품질**로 만듭니다.
교안 하나에 **단계 16 · 실습 16 · 10만 자 안팎**, 그 안의 명령·화면·오류는
**전부 실제로 돌려서** 얻습니다 (§3.0 규율 둘).


## 4. 도구

`tools/guide/` 에 있습니다. 저장소를 어디에 두든 그대로 돌아갑니다.

```bash
python tools/guide/verify_guide.py m09
```
교안 하나를 검수합니다. `<div>` 짝·중첩, 그림 번호 중복과 끊긴 참조, 없는 링크 대상,
실습이 빠진 단계, 채점 항목 누락을 봅니다. **실습을 넣을 때마다 돌립니다.**

```bash
python tools/guide/check_tags.py "guides/*.html"
```
모든 태그의 짝을 봅니다. `verify_guide.py` 는 `<div>` 만 세기 때문에
[m05.html:585](guides/m05.html) 의 군더더기 `</b>` 를 놓쳤습니다. 그래서 따로 만들었습니다.

```bash
cd <실습 조각이 있는 폴더>
python <저장소>/tools/guide/insert_lab.py m09 st8 st9 st10
```
`m09_labs_st8.html` 같은 조각 파일을 읽어 해당 단계 본문 끝에 끼워 넣습니다.
**여는 `<div>` 를 세어 자기 짝을 찾습니다** — 느슨하게 찾으면 바깥 블록의 닫는 태그를
먹습니다(m04 st4 에서 실제로 겪어 st5~st16 이 통째로 중첩됐습니다).

```bash
python tools/guide/audit.py            # 교안 전부
python tools/guide/audit.py m13 m18    # 고른 것만
```
**학생이 혼자 따라할 수 있는가**를 봅니다. `verify_guide.py` 가 구조를 본다면
이것은 따라할 수 있는가를 봅니다 — 다섯 가지입니다.

| 검사 | 무엇을 보나 |
|---|---|
| 전제물 | 받아야 시작되는 것을 쓰면서 «무엇을 받아야 하는지» 를 안 밝혔는가 |
| 앞참조 | 아직 안 한 실습의 **결과물**을 쓰라고 하는가 |
| 막힘 | 실습마다 「안 될 때 볼 곳」 이 있는가 |
| 마감 | 제출물과 「스스로 확인」 이 있는가 |
| 맨손 | 「쳐 보라」 만 있고 「이렇게 나온다」 가 없는가 |

```bash
python .github/scripts/check_site.py
```
사이트 전체의 내부 링크·구조를 봅니다. 교안 하나를 끝낼 때마다 돌립니다.
`exam/m01/img/sample1/02_styleguide.html` 의 외부 CDN 경고 1건은 **원래 있던 것**입니다.

### 새 교안을 시작할 때

`verify_guide.py` 의 `SCORE` 에 그 교안의 채점 항목 번호를 넣어 두십시오.

```python
"m10": [f"{i:02d}" for i in range(1, 16)],   # 항목이 01~15 이면
```

---

## 5. 검증 환경 만들기

교안마다 필요한 것이 다릅니다. 필요할 때만 세우고 끝나면 내립니다.

### 자바 (m08 · m09 · m12 …)

JDK 만 있으면 됩니다. 콘솔에서 `javac -encoding UTF-8` 로 컴파일하고
`java -Dstdout.encoding=UTF-8` 로 돌려야 한글이 안 깨집니다.

### MySQL (m05 · m06 · m09 · m16 …)

```bash
docker run -d --name m09-verify-db -e MYSQL_ROOT_PASSWORD=<암호> -p 3399:3306 mysql:8.0
```
`BookDB` 와 `dbconn` 계정은 교안 m09 실습 2-3 에 적힌 SQL 그대로 만들면 됩니다.
**판을 조심하십시오.** 5.7 과 8.0 은 오류 번호와 기본 문자셋이 다릅니다
(5.7 기본 `latin1`, `WITH`·윈도 함수 없음 / FK 오류 5.7 `1215` · 8.0 `1822`).

### JDBC 드라이버

`mysql-connector-j-*.jar` 가 필요합니다. 이 저장소에는 넣지 않았습니다.
[dev.mysql.com/downloads/connector/j](https://dev.mysql.com/downloads/connector/j/) 에서 받으십시오.

### m09 소켓 채팅 검증용 프로젝트

`ChatServer` · `ChatHandler` · `ChatClient` 와, 클라이언트 둘을 만들어 주고받게 하는
`Demo` 를 만들어 돌렸습니다. 서버 콘솔과 두 클라이언트의 화면이 한 화면에 나오므로
실습 2-10 · 2-11 의 출력을 그대로 얻을 수 있습니다.

> **포트 9999 가 이미 쓰이고 있을 수 있습니다.** 작업하던 PC 에서는 Jupyter 컨테이너가
> 9999 를 쓰고 있어 검증은 9990 으로 했습니다. 교안에 적는 포트는 지시서대로 **9999** 입니다.
> 서버가 포트를 상수에서 읽어 출력하게 해 두면 어느 포트로 돌리든 출력이 정직합니다.

### 교안 그림 (SVG) 만들기

`tools/guide/mkimg_m18.py` 를 돌리면 `guides/img/io_*.svg` 여섯 장을 다시 만듭니다.
그림은 **화면 캡처가 아니라 코드로 그린 것**입니다 — 작업 PC 의 창을 찍지 않습니다.
판면은 기존 교안 그림과 같습니다(흰 바탕 · 검은 선 · Malgun Gothic · 설명은 `#555`).

```bash
python tools/guide/mkimg_m18.py
```

고친 뒤에는 브라우저로 **반드시 눈으로 확인**하십시오. 글자가 칸을 넘거나
화살표 설명이 상자와 겹치는 일이 자주 납니다. 확인하는 가장 빠른 방법:

```bash
python -m http.server 8777 --bind 127.0.0.1
```

### m18 데이터 입출력 검증용 데이터베이스 (`eduio`)

`m09-verify-db` 컨테이너(3399)를 그대로 씁니다. 스크래치패드 `m18/` 에 SQL 과 자바를 두었습니다.

| 파일 | 하는 일 |
|---|---|
| `01_base.sql` | 정규화 이전 장표 표 `apply_sheet` 다섯 줄 |
| `02_phys.sql` | `member` · `teacher` · `course` · `apply` 넷 + 제약조건 |
| `04_bulk.sql` | `apply_log` 50만 줄 (`WITH RECURSIVE`, `cte_max_recursion_depth` 필요) |
| `src/DBUtil.java` | 연결 |
| `src/ApplyDAO.java` | 조회 · 등록 · 수정 · 삭제 |
| `src/TxDemo.java` | 트랜잭션 6 → 7 → 6 |
| `src/BindDemo.java` | SQL 주입 6 / 0 / 6 |
| `src/BatchDemo2.java` | `addBatch` · `rewriteBatchedStatements` 시간 비교 |

> **적재할 때 `--default-character-set=utf8mb4` 를 반드시 붙이십시오.**
> 이 컨테이너는 `character_set_client` 기본값이 `latin1` 이라, 빼면 한글이
> **이중 인코딩**으로 들어갑니다. 터미널에서는 멀쩡해 보이고 **JDBC 로 읽을 때만** 깨집니다.
> 이 현상 자체를 m18 실습 10 에서 교재로 쓰고 있으니, 재현하려면 옵션을 빼고 넣으면 됩니다.

### CSS 판면 확인

교안 페이지는 로그인 가림막이 있어 파일로 바로 못 엽니다. 확인하려면
`auth.js` 관련 두 줄을 지운 사본을 만들어 간단한 정적 서버로 띄우십시오.

---

## 6. 최근에 고친 것 — 다시 건드리지 마십시오

### 판면 (2026-09-18)

`.lab` · `.cmdline` · `h5` 의 CSS 가 **m04 의 인라인 `<style>` 안에만** 있었습니다.
그래서 m05 · m06 · m07 의 실습 46개는 테두리 없이 흘렀고, 여러 줄 명령 83개는
`white-space` 가 없어 **한 줄로 붙어** 있었습니다.

- `assets/guide.css` 로 옮기고 `.cmdline code` 에 `white-space:pre` 를 넣었습니다
  (`pre-wrap` 이 아닙니다 — 삼각형 같은 모양이 좁은 화면에서 흐트러집니다)
- m04 의 인라인 중복 규칙은 지웠습니다. 남겨 두면 인라인이 뒤에 와서 덮어씁니다
- **앞으로 교안에 실습을 넣을 때 따로 CSS 를 넣을 필요가 없습니다**

### 태그

[guides/m05.html:585](guides/m05.html) 의 군더더기 `</b>` 를 지웠습니다.
지금은 `check_tags.py` 기준 전 파일 이상 없습니다.

### 내용 (이미 고침 — 되돌리지 마십시오)

- m04 st8 : 「흐름 줄과 시퀀스 메시지가 1:1」 → 실제로는 4줄 → 10메시지. 「순서가 같아야」로
- m06 : `ERROR 1215` → 8.0 은 **`1822`**
- m06 : 「열여덟 항목」 → 실제 **17개** (배점 합 100 확인함)
- m04 : 그림 번호 `1-2` 중복 → `1-10` 으로
- `guides/sample/bookrent` : 평가도구가 「도서 대여 기능 없음」을 **현행 문제점**으로
  제시하므로, 예제 프로젝트에 대여 기능이 있으면 안 됩니다. 도서 등록·수정·삭제·조회만
  남기고 다시 만들었습니다. 답을 알려 주는 주석도 지웠습니다.

---

## 7. 지켜야 할 제약

이 저장소는 **공개**입니다. `.gitignore` 와 `.github/scripts/check_site.py` 가 막고 있지만
사람이 먼저 지켜야 합니다.

| 올리지 않는 것 | 까닭 |
|---|---|
| `answers/` · `private/` · `plan/` | 평가문항 원본 · 모범답안 · 채점 기준 |
| `students/` · `*.xlsx` | 훈련생 개인정보 (`docs/*.xlsx` 양식은 예외) |
| `guides/lab/` | **실습 1-1 의 답이 그대로 들어 있습니다.** 강사 작업 사본에만 둡니다 |
| `**/reference/*.pdf` | NCS 학습모듈 PDF — 공공누리 제2유형 |
| `NCS-CATALOG/data/learning-modules/` | 위와 같은 까닭 |

- 사이트 로그인은 **보안 장치가 아니라 가림막**입니다. 해시가 공개되어 있습니다.
  진짜로 숨겨야 할 것은 아예 올리지 않습니다.
- 강의실 실습용 Docker 랩은 **강의실 안쪽 망 전용**입니다. 인터넷에 포트를 열지 않고,
  수업이 끝나면 내립니다. 비밀번호가 단순한 것은 그 전제 위에서입니다.

---

## 8. 아직 정하지 못한 것

판단이 필요해 미뤄 둔 것들입니다.

1. **`guides/img/rq_seq.png` 를 다시 그릴지** — 시퀀스 다이어그램의 10번 메시지
   「관리자 알림 발송」에 대응하는 요구사항이 없습니다. 같은 단계(m04 st8)가
   「요구사항에 없는 메시지를 넣지 말라」고 경고하고 있어 서로 어긋납니다.
2. **`guides/img/rq_api.png` 를 다시 그릴지** — 「기존 API」가 세 줄뿐이고
   수정·삭제가 빠져 있습니다. 예제 프로젝트에는 있습니다.
3. **복사 단추와 접기 상자를 나머지 교안에도 넣을지** — 지금은 m04 에만 있습니다
   (`assets/copycode.js`). 「m04 만 두고 나중에」로 정해 두었습니다.
4. **강의실 Docker 랩 컨테이너 정리** — `survey-a` ~ `survey-d` 와 각 DB 여덟 개가
   아직 떠 있습니다. 수업이 끝났으면 `docker compose down` 하십시오.
5. **검증용 컨테이너 `m09-verify-db`** — m09 를 끝내면 지우십시오.
   `docker rm -f m09-verify-db`

---

## 9. 다른 컴퓨터에서 시작하기

```bash
git clone https://github.com/NCS-MODULE-CURRICULUMS/COURSE-MANAGEMENT.git
cd COURSE-MANAGEMENT
python tools/guide/check_tags.py "guides/*.html"     # 0 이어야 합니다
python tools/guide/verify_guide.py m09               # 어디까지 됐는지 확인
python .github/scripts/check_site.py                 # 경고 1건은 원래 있던 것
```

필요한 것 — **Python 3**, **JDK**, **Docker**(MySQL 검증용), **Git**.

`guides/lab/` 은 저장소에 없습니다(위 7절). 강사 작업 사본에서 가져오십시오.

### 아직 커밋하지 않은 변경 (2026-09-18 기준)

작업하던 PC 에 다음이 커밋되지 않은 채 남아 있습니다. **먼저 커밋·푸시해야**
다른 컴퓨터에서 이어서 할 수 있습니다.

```
M  .gitignore                       guides/lab/ 를 막는 줄을 넣음
M  assets/guide.css                 실습 판면 규칙 (6절)
M  guides/m04 m05 m06 m07 m08 m09   실습 본문
D  guides/lab/*                     추적에서 뺌 (파일은 그대로 있습니다)
?? .nojekyll                        GitHub Pages 가 점 파일을 지우지 않게
?? assets/copycode.js               복사 단추
?? guides/img/rq_*.svg (30여 개)    실습용 그림
?? guides/sample/bookrent/          예제 프로젝트
?? tools/guide/                     위 4절의 도구
?? 작업리스트.md                     이 파일
```
